#include<iostream>
#include<iomanip>
#include<algorithm>
#include<cstring>
using namespace std;
typedef struct Node {
	double x, y;
}node;
node a[100005], hen[100005], shu[1000005];
int main()
{
	ios::sync_with_stdio(!cin.tie(NULL));
	cout << setprecision(2) << fixed;
	int n;
	while (cin >> n && n)
	{

	}
	return 0;
}